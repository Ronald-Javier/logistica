<table class="table table-striped table-hover ">
  <thead>
    <tr>
      <th class="text-center">TUS PUNTOS</th>
      <th colspan="3"></th>
    </tr>
  </thead>
<tbody>
    <%= form_with(model: @new_completed_point, local: true) do |form| %>
    <td><%= form.select(:point_id, options_for_select(@points.pluck(:name, :id))) %></td>
    <td><%= form.submit "Completar Punto", class: 'btn btn-light' %></td>
    <% end %>
  </tbody>
</table>

<br>

<%= render  "list" %>